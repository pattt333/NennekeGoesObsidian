---
description: Implement an OpenSpec change and automatically commit the scoped result
---

Use the `openspec-apply-change` workflow. Read all context files, complete and validate pending tasks, run OpenSpec validation, then commit only the change's files after validation succeeds.
